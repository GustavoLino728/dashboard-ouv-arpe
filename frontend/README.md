# Frontend - Dashboard Ouvidoria ARPE

Interface Next.js para consumir a API FastAPI da Ouvidoria ARPE.

```bash
npm install
npm run dev
```

Por padrao, a API esperada e `http://localhost:8001`. Para alterar:

```env
NEXT_PUBLIC_API_URL=http://localhost:8001
```
